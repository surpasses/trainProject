services:
  - type: web
    name: nsw-train-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: MONGO_DB_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0

